# REAL 公开平台协作规则

## 产品定位

本仓库是公开科研展示窗口，不是权威数据库，也不是导师阶段汇报。页面必须面向课题组与同行，保持科研表达、数据可追溯和模型状态透明。

## 禁止事项

- 不上传样品级私有数据库、论文 PDF、论文原图或个人信息；
- 不把计划、模拟数据或预测值写成已经完成的实验/模型结果；
- 在严格分组验证完成前，不开放数值预测；
- 不用漂亮界面替代数据版本、适用域和验证说明；
- 不直接修改来自私有研究库的审计数字。

## 更新流程

1. 先在私有研究库核验数据、模型或论文证据；
2. 生成公开聚合结果，并记录版本和审计日期；
3. 更新 `content/`；
4. 运行 `npm test` 与 `npm run lint`；
5. 检查桌面端和移动端，不泄露私有内容后再发布。

## 当前状态

平台为 Preview 0.1。预测实验室只能输出字段完整度、对应模型层级和缺失字段，必须保留“模型正在进行严格分组验证，暂不提供数值预测”。
